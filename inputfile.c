#include "hsh.h"
/**
  * inputfile - when the argv is a file
  * desc: execute a file
  * @argc: argc
  * @argv: the args
  * @lenv: the list.
  * @execnt: the counter
  * Return: 0
  **/
int inputfile(int argc, char **argv, lenv_s **lenv, unsigned int *execnt)
{
	(void) argc;
	(void) lenv;
	(void) argv;
	(void) execnt;
	return (EXIT_SUCCESS);
}
